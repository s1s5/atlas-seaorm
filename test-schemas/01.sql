CREATE TABLE
    "test_manager" (
        "id" bigserial NOT NULL PRIMARY KEY,
        "name" varchar NOT NULL
    );

CREATE TABLE
    "test_user" (
        "id" bigserial NOT NULL PRIMARY KEY,
        "name" varchar NOT NULL
    );